# Git 提交规则（AI 友好版）

所有提交信息应方便自动代理理解并遵循以下格式：

```
<type>-<scope>-<summary>

- <detail 1>
- <detail 2>
[- <detail 3> ...]
```

## 字段说明

- `type`：必须是 `feature`、`bugfix`、`docs`、`chore`、`refactor` 或 `other` 之一。
- `scope`：指定影响范围或模块，例如 `frontend`、`backend`、`runner`、`build` 等；如涉及多个模块，可使用 `multi`。
- `summary`：一句话概括改动，中文，不超过 72 个字符，使用动词开头（例如 “新增拖放编辑器”）。
- 空行：标题与正文之间保留一行空行。
- 详情列表：使用 2~5 条 `-` 列表项，说明主要改动或关键影响；每条保持 100 字以内，中文；如只需简述两项即可，不必凑满。

## 示例

```
feature-frontend-卡片编辑器

- 新增卡片编辑表单并提供实时校验
- 通过 REST 接口持久化卡片改动
```

```
bugfix-backend-执行循环

- Endless 模式前检查队列是否为空，避免异常等待
- 收到停止信号后及时重置模式为 idle
```

## 禁止事项

- 不允许使用无意义摘要（如 “update files”）。
- 不得省略详情列表或将全部内容写在标题上。
- 避免在摘要或详情中包含敏感密钥、密码等信息。
- 不使用 emoji、全角标点或带色彩的文本。
